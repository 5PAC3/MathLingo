{
  "build": {
    "command": "python3 -m venv /opt/venv && /opt/venv/bin/pip install -r backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && /opt/venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
