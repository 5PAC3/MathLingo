{
  "build": {
    "command": "pip install --break-system-packages -r backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "on_failure"
  }
}
